# Median-columns-Excel

This is a Python script that read specific sheet and columns of an Excel file and takes Median of specific columns.

## Requirements

pip3 install panda
pip3 install xlrd
pip3 install openpyxl
